server:
  port: 9000
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: testdb
      uuid-representation: standard
      chat:
        host: ${spring.data.mongodb.host}
        port: ${spring.data.mongodb.port}
        database: serverChats
